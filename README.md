# Farm tables

Django application. Kartoza task.

## Setting up the django project
The project uses a conda environment with `python 3.11.4`.
To set up the conda environment use:
```python
conda env create --file geo-env.txt
```

To set up the environment use:
```python
pip install -r requirements.txt
```
